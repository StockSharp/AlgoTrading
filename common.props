<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <Import Project="common_versions.props" />

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>

    <!-- The platform repository is a sibling checkout, not a package; see AGENTS.md. -->
    <StockSharpDir>$(MSBuildThisFileDirectory)..\StockSharp (GitHub)\</StockSharpDir>
  </PropertyGroup>
</Project>
